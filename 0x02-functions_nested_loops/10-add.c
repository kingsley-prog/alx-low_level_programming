#include "main.h"
/**
 * add - adds two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: the result of the two integers specify in the function
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
